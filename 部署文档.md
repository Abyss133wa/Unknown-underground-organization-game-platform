# 游戏邀约平台 · 部署文档

## 环境要求

- Python 3.9+，FastAPI + SQLite，无需单独装数据库
- 默认端口 **8000**
- 依赖见 `requirements.txt`

---

## 一、本地部署

```bash
cd web

# 建虚拟环境并激活
python -m venv venv
source venv/bin/activate        # Windows: venv\Scripts\activate

# 装依赖
pip install -r requirements.txt

# 启动
uvicorn app.main:app --reload   # 开发；生产去掉 --reload

# 访问 http://127.0.0.1:8000/
```

---

## 二、服务器部署（CentOS Stream 9）

### 1. 上传并装环境

把 `web` 目录上传到服务器（假设 `/root/web`），然后：

```bash
cd /root/web
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

> CentOS Stream 9 是 PEP 668 外部管理环境，必须用 venv，否则直接 pip 会报 `externally-managed-environment`。

### 2. 启动

```bash
# 后台运行
nohup uvicorn app.main:app --host 0.0.0.0 --port 8000 > uvicorn.log 2>&1 &

# 本地验证
curl http://127.0.0.1:8000/ -I        # 应返回 200
```

### 3. 开放端口（最容易漏的一步）

两层都要放行 8000：

- **云厂商安全组**：添加入方向规则 TCP 8000
- **服务器防火墙**（若已启用）：

```bash
firewall-cmd --permanent --add-port=8000/tcp
firewall-cmd --reload
```

> 现象「进程在跑、本地 curl 通，外网打不开」，基本就是安全组没放行 8000。

### 4. 浏览器访问

```
http://<公网IP>:8000/
```

---

## 三、开机自启（systemd）（未做）

```bash
vi /etc/systemd/system/game-invite.service
```

```ini
[Unit]
Description=Game Invite Platform
After=network.target

[Service]
Type=simple
WorkingDirectory=/root/web
ExecStart=/root/web/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```

> `ExecStart` 必须用 venv 里的 uvicorn 绝对路径。

```bash
systemctl daemon-reload
systemctl enable --now game-invite
systemctl status game-invite
```

---

## 四、更新与备份

```bash
# 更新代码后重启
systemctl restart game-invite

# 备份（数据都在单文件 game_invite.db）
cp game_invite.db game_invite.db.bak-$(date +%Y%m%d)
```

---

## 五、常见问题

| 现象 | 原因 | 解决 |
|---|---|---|
| externally-managed-environment | PEP 668 | 用 venv |
| 外网打不开、本地 curl 通 | 安全组没放行 8000 | 控制台加规则 |
| firewall-cmd: command not found | 未装 firewalld | 可跳过，安全组放行即可 |
| 重启后服务消失 | 用了 nohup | 改用 systemd |

---

## 六、安全提示

- 上线前修改 `app/security.py` 里的默认管理员账号 `root` / 密码 `root7788` 和 `ADMIN_TOKEN`
- 若服务器 root 密码曾明文暴露，请立即更换
- 大陆长期对外运营建议域名 + ICP 备案（个人即可）

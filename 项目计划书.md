# 游戏邀约平台 · 项目计划书

> 面向 150 人左右小群聊的游戏组队/招募规划工具
> 目标：让群友方便地发起游戏招募、约定时间、自由报名参与，仅用于规划，不做游戏本体。

---

## 1. 项目概述

**一句话定位**：一个轻量的、手机 / PC 浏览器均可打开的「游戏招募 + 报名」网页应用（响应式，双端适配）。

- **发起人**在平台上发布一次游戏招募（玩什么、几点、几个人、在哪开黑），生成一条招募信息。
- **群友**打开群里的链接，看到当前所有招募，点一下就能报名参加，也能取消。
- **发起人**随时查看报名情况，到点了开玩。

**不做的事**：不做游戏本身、不做匹配系统、不做付费、不做复杂社交。核心价值是「省去在群里爬楼、刷屏、接龙」。

---

## 2. 目标与范围

### 2.1 目标
| 目标 | 说明 |
|---|---|
| 降低组织成本 | 一条链接替代群内接龙/爬楼 |
| 清晰可见 | 谁发起了什么、几点、还差几个人，一眼看清 |
| 零门槛 | 免注册，填个昵称即可参与 |

### 2.2 范围（MVP 必做）
- 发起 / 查看 / 报名 / 取消报名 / 结束招募
- 招募状态（招募中 / 已满 / 已结束）
- 人数上限与超员保护
- 简单的管理口令（发起人用来编辑/删除自己的招募）

### 2.3 非目标（本期不做）
- 账号体系、微信/手机登录
- 消息推送 / 站内通知
- 支付、等级、积分
- 游戏内数据对接

---

## 3. 用户与使用场景

- **用户**：群主 / 管理员、爱组局的活跃群友、普通参与者。
- **典型流程**：
  1. 小明想今晚 8 点开一局「英雄联盟」，打开平台 → 发起招募 → 填好信息 → 得到一条招募 + 一个管理口令。
  2. 小明把平台链接（或该招募的链接）发到群里。
  3. 群友点开，看到「英雄联盟 · 今晚 20:00 · 3/5 人」，点「报名」，输入昵称完成。
  4. 人到齐，开玩；结束后小明把招募标记为「已结束」。

---

## 4. 技术选型

### 4.1 语言/框架结论：**Python + FastAPI**

| 维度 | Python (FastAPI) | Java (Spring Boot) |
|---|---|---|
| 规模适配 | ✅ 150 人极小规模，轻松应对 | 能力过剩 |
| 开发速度 | ✅ 代码少、迭代快 | 相对繁琐 |
| 维护成本 | ✅ 个人/小团队友好 | 工程规范重 |
| 生态 | ✅ 定时/日期/数据库库齐全 | 也很成熟 |
| 部署 | ✅ 单进程即可 | 需要 JVM，资源占用更高 |

**结论**：本项目为轻量 CRUD + 日程 + 报名，选 **Python + FastAPI**。除非团队更熟 Java 或计划扩展为大型平台，否则不选 Java。

### 4.2 技术栈明细

| 层 | 选型 | 理由 |
|---|---|---|
| 后端框架 | FastAPI | 快、自动生成 API 文档、异步 |
| 数据库 | SQLite（起步）→ PostgreSQL（可选） | SQLite 零部署成本，够用；有需要再迁 |
| ORM | SQLAlchemy | 标准、成熟 |
| 前端 | 原生 HTML + 轻量 JS（或 Vue3 单文件） | 页面简单，响应式适配 |
| 样式 | 手写 CSS / Tailwind | 响应式，手机 / PC 双端友好 |
| 部署 | 单机 + Uvicorn + Nginx（或云服务器一键） | 最简 |
| 定时/到期 | APScheduler 或手动状态判断 | 自动把过期招募标记结束 |

> 前端不必引入复杂工程化：一个静态页 + 少量 JS 调 API 即可，方便快速上线。

---

## 5. 功能需求

### 5.1 招募管理（发起人）
- 创建招募：游戏名称、游戏类型、人数上限、开始时间、结束时间、开黑渠道（YY/QQ 频道/游戏内房间号）、备注。
- 编辑招募（需管理口令）。
- 结束 / 删除招募（需管理口令）。

### 5.2 报名（参与者）
- 查看招募列表（可按状态、时间筛选）。
- 报名参与（填昵称，可留一句备注）。
- 取消报名。
- 查看「我报名的游戏」。

### 5.3 规则与保护
- 人数上限：满员后拒绝新报名。
- 防重复：同一昵称不能在同一招募重复报名。
- 时间冲突提示：同一昵称报名了两个时间重叠的招募时给出提示（软提示，不强制）。
- 状态自动流转：到达结束时间后自动置为「已结束」。

### 5.4 边界情况
- 过期招募自动下架/归档。
- 昵称长度限制、非法输入过滤（防注入/脚本）。

---

## 6. 系统架构

```
手机/浏览器 (群友)
      │  HTTP(S)
      ▼
┌──────────────────────────┐
│  Nginx（可选，反向代理/HTTPS）│
└──────────────────────────┘
      │
      ▼
┌──────────────────────────┐
│   FastAPI 应用            │
│  ├─ 路由/接口层           │
│  ├─ 业务逻辑（报名校验等）  │
│  └─ ORM (SQLAlchemy)     │
└──────────────────────────┘
      │
      ▼
┌──────────────────────────┐
│  SQLite / PostgreSQL      │
└──────────────────────────┘
```

- 单进程、单体应用，无微服务、无消息队列。
- 前端为静态页，可与后端同源部署，或独立托管。

---

## 7. 数据库设计

### 7.1 表：`games`（招募/活动）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | int / PK | 自增主键 |
| title | str | 游戏名称 |
| game_type | str | 游戏类型（可选） |
| description | text | 备注说明 |
| start_time | datetime | 开始时间 |
| end_time | datetime | 结束时间 |
| max_players | int | 人数上限 |
| channel | str | 开黑渠道/房间号（可选） |
| creator_nickname | str | 发起人昵称 |
| admin_token_hash | str | 管理口令哈希（用于编辑/删除） |
| status | enum | 招募中 / 已满 / 已结束 |
| created_at | datetime | 创建时间 |

### 7.2 表：`signups`（报名）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | int / PK | 自增主键 |
| game_id | int / FK | 关联 games.id |
| nickname | str | 参与者昵称 |
| remark | str | 备注（可选） |
| created_at | datetime | 报名时间 |

- 唯一约束：`UNIQUE(game_id, nickname)`，保证同一招募不重复报名。
- 索引：`game_id`、`start_time`。

---

## 8. API 接口设计（REST）

| 方法 | 路径 | 说明 | 是否需口令 |
|---|---|---|---|
| GET | `/api/games` | 招募列表（支持 ?status= & ?upcoming= 筛选） | 否 |
| GET | `/api/games/{id}` | 招募详情 + 报名名单 | 否 |
| POST | `/api/games` | 发起招募 | 否（返回管理口令） |
| PATCH | `/api/games/{id}` | 编辑招募 | ✅ |
| DELETE | `/api/games/{id}` | 删除/结束招募 | ✅ |
| POST | `/api/games/{id}/join` | 报名 | 否 |
| DELETE | `/api/games/{id}/join` | 取消报名 | 否（按昵称） |
| GET | `/api/me?nickname=` | 我报名的游戏 | 否 |

> 管理口令：创建时随机生成并仅展示一次给发起人；编辑/删除时携带校验，避免免注册下被他人乱改。

---

## 9. 前端页面设计（响应式，手机 / PC 双端适配）

1. **首页 / 招募列表**：卡片式展示每个招募（游戏名、时间、`x/y 人`、状态标签），顶部筛选（进行中/即将开始/已结束）。
2. **发起招募页**：表单（游戏名、时间、人数、渠道、备注）。
3. **招募详情页**：完整信息 + 报名按钮 + 已报名名单。
4. **报名弹窗**：输入昵称 +（可选）备注。
5. **我的报名**：查看自己报名的游戏，可取消。

### 响应式适配要点
- **手机端（窄屏）**：卡片单列堆叠、按钮大而清晰、触控友好，操作路径 ≤ 3 步。
- **PC 端（宽屏）**：招募列表用多列网格（grid）呈现，信息密度更高；详情页可左右分栏（左侧信息、右侧报名名单）。
- **断点方案**：以 `max-width` 媒体查询为主，常用断点约 `640px / 1024px`，小屏下卡片自动收窄为单列、表格转卡片。
- **共享同一套页面与 API**，不做两套前端，降低维护成本。

---

## 10. 开发计划（里程碑）

| 阶段 | 内容 | 预计工作量 |
|---|---|---|
| M1 骨架 | 搭 FastAPI 项目、数据库模型、基础路由 | 0.5 天 |
| M2 核心接口 | 发起/报名/取消/列表/详情 API | 1 天 |
| M3 前端页面 | 移动端页面 + 调通 API | 1.5 天 |
| M4 规则保护 | 满员/防重复/口令/状态流转 | 0.5 天 |
| M5 联调与部署 | 本地联调 → 云服务器上线 → 群内试运行 | 1 天 |
| **合计** | | **约 4.5 个工作日** |

> 建议 M5 后先在群内小范围试运行 1 周，收集反馈再迭代。

---

## 11. 部署方案

- **起步（最低成本）**：一台轻量云服务器（或家里一台常开机器的内网穿透），`uvicorn` 跑后端 + Nginx 提供 HTTPS 与静态页，SQLite 单文件存储。
- **域名/证书**：申请一个简单域名 + Let's Encrypt 免费 HTTPS（手机浏览器打开更友好）。
- **数据备份**：定期备份 SQLite 文件即可（`cp`/定时脚本）。
- **可选升级**：用户量/数据增多后，把 SQLite 换成 PostgreSQL。

---

## 12. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 免注册导致乱报名/刷屏 | 体验下降 | 昵称限长、同一招募去重、必要时加 IP 频率限制 |
| 时间冲突（同一人报两个重叠局） | 爽约 | 报名时软提示；不强制，避免复杂化 |
| 并发满员（多人同时抢最后一个名额） | 超员 | 后端事务 + 唯一/计数校验，原子化处理 |
| 服务器宕机 | 服务不可用 | 起步阶段单机可接受；SQLite 好备份 |
| 恶意注入/脚本 | 安全 | 输入过滤、转义、口令哈希存储 |

---

## 13. 后续扩展方向（非本期）

- 微信登录 / 微信内分享卡片
- 站内消息与开团提醒
- 常用游戏模板、一键复制上周的局
- 统计（谁最活跃、最常玩的游戏）
- 日历视图 / 订阅到系统日历

---

*文档版本 v1.0 · 2026-09-02*
